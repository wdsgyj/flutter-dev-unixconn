## 0.0.1

* Expose the native Go per-request `requestId` on `UnixProxyTraceEvent`.
* Add iOS/macOS-safe unix socket path generation helpers.
* Add iOS integration coverage for proxy requests and trace events.
